---
name: artifacts-builder
description: Builds and validates complex HTML artifacts, bundling the sources before publishing.
allowed-tools: Read, Write, Edit, Bash
---

# Artifacts builder

Build the bundle with `scripts/build.js`, then validate the output.
