---
name: using-git-worktrees
description: Creates an isolated git worktree so changes can be developed without touching the main checkout.
allowed-tools: Read, Edit, Write, Bash
---

# Using git worktrees

Create the worktree, run the work inside it, then remove it when the branch is merged.
